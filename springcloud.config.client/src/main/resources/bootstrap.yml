spring:
  application:
    name: eureka-server
  cloud:
    config:
      uri:
        - http://localhost:9999/
      profile: dev
      label: master
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest